
@{
    ViewData["Title"] = "Parcial";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1 class="text-center" >Parcial</h1>

<table class="table table-dark table-hover">
    <thead>
    <th>
        Primera Parte
    </th>
    <th>
        Segunda Parte
    </th>
    <th>
        Tercera Parte
    </th>
    </thead>
    <tbody>
        <tr>
            <td>@Html.ActionLink("Students", "Index", "Students", new {}, new { @class = "btn btn-primary" })</td>
            <td>@Html.ActionLink("Course", "Index", "Course", new {}, new { @class = "btn btn-primary" })</td>
            <td>@Html.ActionLink("Enrollment", "Index", "Enrollment", new { }, new { @class = "btn btn-primary" })</td>
            
        </tr>
    </tbody>
</table>

